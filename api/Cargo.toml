[package]
name = "api"
version = "0.0.0-alpha"
edition.workspace = true
license.workspace = true

[dependencies]
thiserror = "1.0"
tonic = { version = "0.11", features = ["transport", "codegen", "prost"] }
prost = "0.12"

[build-dependencies]
tonic-build = "0.11"
